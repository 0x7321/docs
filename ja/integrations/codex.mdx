---
title: "OpenAI Codex CLI"
description: "OpenAIのCodex CLIでBearLabを使用します。"
---

[Codex CLI](https://github.com/openai/codex)はOpenAIの公式コマンドラインコーディングエージェントで、カスタムプロバイダー経由で接続されます。

## 設定

`~/.codex/config.toml`を編集し、BearLabを指すプロバイダーを追加します:

```toml
model = "gpt-4o"
model_provider = "bearlab"

# サードパーティプロバイダーはレスポンスストレージを無効にする必要があります
disable_response_storage = true

[model_providers.bearlab]
name = "BearLab"
base_url = "https://bearlab.ai/v1"
env_key = "BEARLAB_API_KEY"
wire_api = "responses"
```

次にキーをエクスポートして実行します:

```bash
export BEARLAB_API_KEY="YOUR_API_KEY"
codex
```

<Warning>
  OpenAI互換アドレス(`/v1`)を使用してください。サードパーティプロバイダーは `wire_api = "responses"`(Codex が唯一サポートするプロトコル)と `disable_response_storage = true` を設定する必要があります。そうしないと一部の機能がエラーになります。`model`は[modelsエンドポイント](/ja/guides/models)に存在する必要があります。
</Warning>
