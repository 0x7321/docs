---
title: "Token 列表"
description: "GET /api/token/"
---

接口：`GET /api/token/`

| 參數 | 說明 |
| --- | --- |
| `p` | 頁碼 |
| `page_size` | 每頁數量 |
| `keyword` | 名稱模糊搜尋 |

查詢當前帳號所有 API Key。

```bash
curl "https://bearlab.ai/api/token/?p=1&page_size=10" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

返回 items 陣列，`key` 已脫敏（首 4 + 末 4 位），`status` 1=啟用/2=禁用/3=過期，`expired_time: -1` 永不過期。
